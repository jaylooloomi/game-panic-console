# Phase 0 垂直切片 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 做出一個單機可玩的垂直切片，驗證「每 20 秒強制切換 3 個同時運行的小遊戲」是否好玩。

**Architecture:** 所有遊戲邏輯寫成**不依賴 UnityEngine 的純 C#**（`PanicConsole.Core` + `PanicConsole.Minigames` 兩個 asmdef），可用 Unity EditMode 測試與 `dotnet test` 雙重驗證；Unity 的 `MonoBehaviour` 只當渲染 + 輸入的薄殼。核心由純物件 `SwitchEngine` 編排：持有 `SwitchTimer`（20→15→10 秒曲線）、`MatchState`（HP）、3 個 `IMinigame`，每幀 `Tick` 全部小遊戲、切換焦點、處理失敗扣血。

**Tech Stack:** Unity 6 LTS（或 2022.3 LTS）、C#、Unity Test Framework（NUnit, EditMode）。純邏輯另可由 .NET SDK `dotnet test` 驗證。

> 對 spec 的小幅精修：spec 草稿把 `MinigameBase` 寫成 `: MonoBehaviour`，本計畫改為**純 C# 抽象類別**（spec 第 4.1 節本來就要求「核心模擬寫成可獨立測試的純 C# 類別、MonoBehaviour 僅作外殼」），渲染交給對應的 `MinigameView : MonoBehaviour`。其餘與 spec 一致。

> **✅ 實作進度（2026-06-14）：Task 0–7 的純 C# 核心已預先實作並用 `dotnet test` 驗證（35/35 全綠）。** 程式碼已位於 `Assets/Scripts/Core`、`Assets/Scripts/Minigames`、`Assets/Tests/EditMode`，asmdef 已備妥。驗證用的 dotnet 測試專案在 `tools/CoreTests`（`dotnet test tools/CoreTests` 可隨時重跑，不需 Unity）。Unity 安裝後：以 Hub「Add project from disk」開啟本 repo 根目錄 → Test Runner 直接 Run EditMode 應全綠 → 接續 **Task 8–11**（MonoBehaviour 殼、View、場景、試玩）。下方 Task 1–7 的程式碼即最終版（已含預建期間發現並修正的兩處測試與 DinoSim 跳躍判定順序）。

> **Commit 慣例：** 每個 commit 訊息結尾加一行 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`（下方範例為求精簡省略）。

---

## File Structure

```
Assets/
  Scripts/
    Core/                         # 純 C#，無 UnityEngine 依賴
      PanicConsole.Core.asmdef
      IMinigame.cs                # 小遊戲合約
      MinigameBase.cs             # 抽象基底（OnFail 管線 + Focus 狀態）
      SwitchTimer.cs              # 20秒倒數 + 3秒警報 + 20/15/10 曲線
      MatchState.cs               # HP / 生存時間 / 分數 / GameOver
      SwitchEngine.cs             # 核心編排（持有 timer/state/games）
    Minigames/                    # 純 C#，依賴 Core
      PanicConsole.Minigames.asmdef
      DinoSim.cs                  # 反射：閃避障礙
      SnakeSim.cs                 # 空間：貪食蛇
      PianoSim.cs                 # 節奏：落下方塊
    Views/                        # Unity 渲染薄殼
      PanicConsole.Views.asmdef
      MinigameView.cs             # 抽象 view（持有 IMinigame，每幀畫）
      DinoView.cs / SnakeView.cs / PianoView.cs
    App/                          # Unity 入口與 UI
      PanicConsole.App.asmdef
      SwitchController.cs         # MonoBehaviour：每幀 Tick SwitchEngine
      InputRouter.cs              # 只把輸入送給前台 view
      DashboardUI.cs              # HP/計時/分數/警報顯示
      DebugHud.cs                 # 可調切換秒數的滑桿
      GameBootstrap.cs            # 在場景中組裝一切
  Scenes/
    Slice.unity
  Tests/
    EditMode/
      PanicConsole.Tests.EditMode.asmdef
      Fakes/FakeMinigame.cs
      SwitchTimerTests.cs
      MatchStateTests.cs
      MinigameBaseTests.cs
      SwitchEngineTests.cs
      DinoSimTests.cs
      SnakeSimTests.cs
      PianoSimTests.cs
```

**測試執行方式（兩種）**
- Unity 內：`Window > General > Test Runner > EditMode > Run All`
- CLI：`& "<UnityEditor路徑>\Unity.exe" -batchmode -projectPath . -runTests -testPlatform EditMode -testResults TestResults.xml -logFile -`
- （純邏輯亦可：在一個引用 `Core`/`Minigames` 原始碼的 `dotnet` 測試專案執行 `dotnet test`。）

---

## Task 0：建立 Unity 專案與組件骨架

**Files:**
- Create: `Assets/Scripts/Core/PanicConsole.Core.asmdef`
- Create: `Assets/Scripts/Minigames/PanicConsole.Minigames.asmdef`
- Create: `Assets/Scripts/Views/PanicConsole.Views.asmdef`
- Create: `Assets/Scripts/App/PanicConsole.App.asmdef`
- Create: `Assets/Tests/EditMode/PanicConsole.Tests.EditMode.asmdef`

- [ ] **Step 1：建立 Unity 專案**
用 Unity Hub 以 **2D (Built-in/URP 皆可)** 範本建立專案，專案放在 repo 根目錄（讓 `Assets/` 與現有 `docs/` 同層）。

- [ ] **Step 2：建立資料夾與 asmdef**
依上方 File Structure 建立資料夾。各 asmdef 內容：

`PanicConsole.Core.asmdef`
```json
{ "name": "PanicConsole.Core", "rootNamespace": "PanicConsole.Core", "references": [], "autoReferenced": true }
```
`PanicConsole.Minigames.asmdef`
```json
{ "name": "PanicConsole.Minigames", "rootNamespace": "PanicConsole.Minigames", "references": ["PanicConsole.Core"], "autoReferenced": true }
```
`PanicConsole.Views.asmdef`
```json
{ "name": "PanicConsole.Views", "references": ["PanicConsole.Core", "PanicConsole.Minigames"] }
```
`PanicConsole.App.asmdef`
```json
{ "name": "PanicConsole.App", "references": ["PanicConsole.Core", "PanicConsole.Minigames", "PanicConsole.Views"] }
```
`PanicConsole.Tests.EditMode.asmdef`
```json
{
  "name": "PanicConsole.Tests.EditMode",
  "references": ["UnityEngine.TestRunner", "UnityEditor.TestRunner", "PanicConsole.Core", "PanicConsole.Minigames"],
  "includePlatforms": ["Editor"],
  "precompiledReferences": ["nunit.framework.dll"],
  "autoReferenced": false,
  "defineConstraints": ["UNITY_INCLUDE_TESTS"]
}
```

- [ ] **Step 3：驗證專案可開、Test Runner 認得測試組件**
開啟 `Window > General > Test Runner`，確認 EditMode 分頁出現（即使尚無測試）。Console 無編譯錯誤。

- [ ] **Step 4：Commit**
```bash
git add Assets ProjectSettings Packages
git commit -m "chore(slice): scaffold Unity project and assembly definitions"
```

---

## Task 1：SwitchTimer（純邏輯，TDD）

**Files:**
- Create: `Assets/Scripts/Core/SwitchTimer.cs`
- Test: `Assets/Tests/EditMode/SwitchTimerTests.cs`

- [ ] **Step 1：寫失敗測試**
```csharp
using NUnit.Framework;
using PanicConsole.Core;

public class SwitchTimerTests
{
    [Test] public void StartsAtRoundOneWith20Seconds()
    {
        var t = new SwitchTimer();
        Assert.AreEqual(1, t.Round);
        Assert.AreEqual(20f, t.Remaining, 0.001f);
    }

    [Test] public void IntervalCurveByRound()
    {
        Assert.AreEqual(20f, SwitchTimer.IntervalForRound(1), 0.001f);
        Assert.AreEqual(20f, SwitchTimer.IntervalForRound(3), 0.001f);
        Assert.AreEqual(15f, SwitchTimer.IntervalForRound(4), 0.001f);
        Assert.AreEqual(15f, SwitchTimer.IntervalForRound(6), 0.001f);
        Assert.AreEqual(10f, SwitchTimer.IntervalForRound(7), 0.001f);
        Assert.AreEqual(10f, SwitchTimer.IntervalForRound(99), 0.001f);
    }

    [Test] public void TickReducesRemaining()
    {
        var t = new SwitchTimer();
        t.Tick(5f);
        Assert.AreEqual(15f, t.Remaining, 0.001f);
    }

    [Test] public void WarningFiresOnceWhenCrossingThreshold()
    {
        var t = new SwitchTimer();
        int warnings = 0; t.OnWarning += () => warnings++;
        t.Tick(16.5f); // remaining 3.5 -> no warn
        Assert.AreEqual(0, warnings);
        t.Tick(1f);    // remaining 2.5 -> warn
        Assert.AreEqual(1, warnings);
        t.Tick(0.5f);  // still in warning zone, no extra
        Assert.AreEqual(1, warnings);
    }

    [Test] public void SwitchFiresAtZeroAndAdvancesRound()
    {
        var t = new SwitchTimer();
        int switches = 0; t.OnSwitch += () => switches++;
        t.Tick(20f);
        Assert.AreEqual(1, switches);
        Assert.AreEqual(2, t.Round);
        Assert.AreEqual(20f, t.Remaining, 0.001f); // round 2 still 20
    }

    [Test] public void WarningReArmsAfterSwitch()
    {
        // 以小步前進（接近真實逐幀），避免單一大步同時跨越警報與切換
        var t = new SwitchTimer();
        int warnings = 0; t.OnWarning += () => warnings++;
        t.Tick(17f); // remaining 3 -> warn (round 1)
        Assert.AreEqual(1, warnings);
        t.Tick(3f);  // remaining 0 -> switch to round 2, re-arm
        t.Tick(18f); // remaining 2 -> warn again (round 2)
        Assert.AreEqual(2, warnings);
    }
}
```

- [ ] **Step 2：跑測試確認失敗**
Test Runner > Run All。預期：編譯失敗／`SwitchTimer` 未定義。

- [ ] **Step 3：實作**
```csharp
using System;

namespace PanicConsole.Core
{
    /// <summary>20 秒強制切換的倒數計時器：管理輪數、警報與切換事件。</summary>
    public class SwitchTimer
    {
        public float WarningSeconds { get; }
        public int Round { get; private set; }
        public float Remaining { get; private set; }
        public bool WarningActive => Remaining <= WarningSeconds;

        public event Action OnWarning; // 進入倒數 3 秒時觸發一次
        public event Action OnSwitch;  // 倒數歸零時觸發

        private bool _warningFired;

        public SwitchTimer(float warningSeconds = 3f)
        {
            WarningSeconds = warningSeconds;
            Round = 1;
            Remaining = IntervalForRound(Round);
        }

        public static float IntervalForRound(int round)
        {
            if (round <= 3) return 20f;
            if (round <= 6) return 15f;
            return 10f;
        }

        public void Tick(float dt)
        {
            if (dt < 0f) throw new ArgumentOutOfRangeException(nameof(dt));
            Remaining -= dt;

            if (!_warningFired && Remaining <= WarningSeconds)
            {
                _warningFired = true;
                OnWarning?.Invoke();
            }

            if (Remaining <= 0f)
            {
                Round++;
                Remaining = IntervalForRound(Round);
                _warningFired = false;
                OnSwitch?.Invoke();
            }
        }
    }
}
```

- [ ] **Step 4：跑測試確認通過** — Test Runner > Run All，全綠。
- [ ] **Step 5：Commit**
```bash
git add Assets/Scripts/Core/SwitchTimer.cs Assets/Tests/EditMode/SwitchTimerTests.cs
git commit -m "feat(core): SwitchTimer with 20/15/10 curve and warning"
```

---

## Task 2：MatchState（純邏輯，TDD）

**Files:**
- Create: `Assets/Scripts/Core/MatchState.cs`
- Test: `Assets/Tests/EditMode/MatchStateTests.cs`

- [ ] **Step 1：寫失敗測試**
```csharp
using NUnit.Framework;
using PanicConsole.Core;

public class MatchStateTests
{
    [Test] public void StartsWithFullHp()
    {
        var s = new MatchState();
        Assert.AreEqual(5, s.MaxHp);
        Assert.AreEqual(5, s.Hp);
        Assert.IsFalse(s.IsGameOver);
    }

    [Test] public void LoseHpDecrements()
    {
        var s = new MatchState();
        s.LoseHp();
        Assert.AreEqual(4, s.Hp);
    }

    [Test] public void GameOverWhenHpReachesZero()
    {
        var s = new MatchState(2);
        int over = 0; s.OnGameOver += () => over++;
        s.LoseHp(); s.LoseHp();
        Assert.IsTrue(s.IsGameOver);
        Assert.AreEqual(1, over);
    }

    [Test] public void HpNeverNegativeAndLoseAfterOverIsNoop()
    {
        var s = new MatchState(1);
        s.LoseHp(); s.LoseHp();
        Assert.AreEqual(0, s.Hp);
    }

    [Test] public void ScoreAndTimeAccumulateUntilGameOver()
    {
        var s = new MatchState(1);
        s.AddScore(10); s.TickTime(2f);
        Assert.AreEqual(10, s.Score);
        Assert.AreEqual(2f, s.SurvivalTime, 0.001f);
        s.LoseHp(); // game over
        s.AddScore(10); s.TickTime(2f);
        Assert.AreEqual(10, s.Score);
        Assert.AreEqual(2f, s.SurvivalTime, 0.001f);
    }
}
```

- [ ] **Step 2：跑測試確認失敗** — 預期 `MatchState` 未定義。

- [ ] **Step 3：實作**
```csharp
using System;

namespace PanicConsole.Core
{
    /// <summary>對局共享狀態：HP、生存時間、分數、勝負。</summary>
    public class MatchState
    {
        public int MaxHp { get; }
        public int Hp { get; private set; }
        public float SurvivalTime { get; private set; }
        public int Score { get; private set; }
        public bool IsGameOver => Hp <= 0;

        public event Action OnGameOver;

        public MatchState(int maxHp = 5)
        {
            MaxHp = maxHp;
            Hp = maxHp;
        }

        public void LoseHp(int amount = 1)
        {
            if (IsGameOver) return;
            Hp = Math.Max(0, Hp - amount);
            if (IsGameOver) OnGameOver?.Invoke();
        }

        public void AddScore(int points) { if (!IsGameOver) Score += points; }
        public void TickTime(float dt) { if (!IsGameOver) SurvivalTime += dt; }
    }
}
```

- [ ] **Step 4：跑測試確認通過**
- [ ] **Step 5：Commit**
```bash
git add Assets/Scripts/Core/MatchState.cs Assets/Tests/EditMode/MatchStateTests.cs
git commit -m "feat(core): MatchState HP/score/time with game-over"
```

---

## Task 3：IMinigame + MinigameBase（純邏輯，TDD）

**Files:**
- Create: `Assets/Scripts/Core/IMinigame.cs`
- Create: `Assets/Scripts/Core/MinigameBase.cs`
- Create: `Assets/Tests/EditMode/Fakes/FakeMinigame.cs`
- Test: `Assets/Tests/EditMode/MinigameBaseTests.cs`

- [ ] **Step 1：寫失敗測試（含測試替身）**
`Fakes/FakeMinigame.cs`
```csharp
using PanicConsole.Core;

public class FakeMinigame : MinigameBase
{
    public int SimTicks;
    public int ResetCount;
    public bool LastTickFocused;

    public FakeMinigame(string id = "fake") { GameId = id; }
    public override void Reset() { ResetCount++; }
    protected override void Simulate(float dt, bool isFocused)
    {
        SimTicks++;
        LastTickFocused = isFocused;
    }
    public void FailNow() => TriggerFail();
}
```
`MinigameBaseTests.cs`
```csharp
using NUnit.Framework;
using PanicConsole.Core;

public class MinigameBaseTests
{
    [Test] public void TickInvokesSimulateWithFocusFlag()
    {
        var g = new FakeMinigame();
        g.Tick(0.1f, true);
        Assert.AreEqual(1, g.SimTicks);
        Assert.IsTrue(g.LastTickFocused);
        g.Tick(0.1f, false);
        Assert.AreEqual(2, g.SimTicks);
        Assert.IsFalse(g.LastTickFocused);
    }

    [Test] public void FocusBlurTogglesIsFocused()
    {
        var g = new FakeMinigame();
        g.OnFocus(); Assert.IsTrue(g.IsFocused);
        g.OnBlur();  Assert.IsFalse(g.IsFocused);
    }

    [Test] public void TriggerFailRaisesOnFailWithSelf()
    {
        var g = new FakeMinigame();
        IMinigame failed = null;
        g.OnFail += m => failed = m;
        g.FailNow();
        Assert.AreSame(g, failed);
    }
}
```

- [ ] **Step 2：跑測試確認失敗** — 預期 `IMinigame`/`MinigameBase` 未定義。

- [ ] **Step 3：實作**
`IMinigame.cs`
```csharp
using System;

namespace PanicConsole.Core
{
    /// <summary>所有小遊戲（含未來玩家自製）共同實作的合約。</summary>
    public interface IMinigame
    {
        string GameId { get; }
        bool IsFocused { get; }
        event Action<IMinigame> OnFail;

        void Init();
        void Reset();
        void OnFocus();
        void OnBlur();
        void Tick(float dt, bool isFocused); // 每幀呼叫，不論前後台
    }
}
```
`MinigameBase.cs`
```csharp
using System;

namespace PanicConsole.Core
{
    public abstract class MinigameBase : IMinigame
    {
        public string GameId { get; protected set; }
        public bool IsFocused { get; private set; }
        public event Action<IMinigame> OnFail;

        public virtual void Init() { }
        public abstract void Reset();
        public virtual void OnFocus() => IsFocused = true;
        public virtual void OnBlur() => IsFocused = false;

        public void Tick(float dt, bool isFocused) => Simulate(dt, isFocused);

        protected abstract void Simulate(float dt, bool isFocused);
        protected void TriggerFail() => OnFail?.Invoke(this);
    }
}
```

- [ ] **Step 4：跑測試確認通過**
- [ ] **Step 5：Commit**
```bash
git add Assets/Scripts/Core/IMinigame.cs Assets/Scripts/Core/MinigameBase.cs Assets/Tests/EditMode/Fakes/FakeMinigame.cs Assets/Tests/EditMode/MinigameBaseTests.cs
git commit -m "feat(core): IMinigame contract and MinigameBase"
```

---

## Task 4：SwitchEngine（純邏輯，核心編排，TDD）

**Files:**
- Create: `Assets/Scripts/Core/SwitchEngine.cs`
- Test: `Assets/Tests/EditMode/SwitchEngineTests.cs`

- [ ] **Step 1：寫失敗測試**
```csharp
using System.Collections.Generic;
using NUnit.Framework;
using PanicConsole.Core;

public class SwitchEngineTests
{
    static SwitchEngine Make(out List<FakeMinigame> games, int hp = 5)
    {
        games = new List<FakeMinigame> { new FakeMinigame("a"), new FakeMinigame("b"), new FakeMinigame("c") };
        var list = new List<IMinigame>(games);
        return new SwitchEngine(list, new SwitchTimer(), new MatchState(hp));
    }

    [Test] public void StartFocusesFirstGameOnly()
    {
        var e = Make(out var games);
        e.Start();
        Assert.AreEqual(0, e.FocusIndex);
        Assert.IsTrue(games[0].IsFocused);
        Assert.IsFalse(games[1].IsFocused);
        Assert.IsFalse(games[2].IsFocused);
    }

    [Test] public void AllGamesTickEveryFrameRegardlessOfFocus()
    {
        var e = Make(out var games);
        e.Start();
        e.Tick(0.1f);
        Assert.AreEqual(1, games[0].SimTicks);
        Assert.AreEqual(1, games[1].SimTicks);
        Assert.AreEqual(1, games[2].SimTicks);
    }

    [Test] public void FocusAdvancesAfterInterval()
    {
        var e = Make(out var games);
        e.Start();
        e.Tick(20f); // cross the 20s interval
        Assert.AreEqual(1, e.FocusIndex);
        Assert.IsFalse(games[0].IsFocused);
        Assert.IsTrue(games[1].IsFocused);
    }

    [Test] public void FailDeductsHpAndResetsThatGame()
    {
        var e = Make(out var games);
        e.Start();
        int before = games[1].ResetCount; // Start() 已呼叫過一次 Reset
        games[1].FailNow();
        Assert.AreEqual(4, e.State.Hp);
        Assert.AreEqual(before + 1, games[1].ResetCount);
    }

    [Test] public void TickIsNoopAfterGameOver()
    {
        var e = Make(out var games, hp: 1);
        e.Start();
        games[0].FailNow(); // hp 1 -> 0 -> game over
        int before = games[0].SimTicks;
        e.Tick(0.1f);
        Assert.AreEqual(before, games[0].SimTicks);
    }
}
```

- [ ] **Step 2：跑測試確認失敗** — 預期 `SwitchEngine` 未定義。

- [ ] **Step 3：實作**
```csharp
using System;
using System.Collections.Generic;

namespace PanicConsole.Core
{
    /// <summary>核心循環編排：每幀推進計時器與所有小遊戲、切換焦點、處理失敗扣血。</summary>
    public class SwitchEngine
    {
        private readonly IReadOnlyList<IMinigame> _games;
        public int FocusIndex { get; private set; }
        public SwitchTimer Timer { get; }
        public MatchState State { get; }
        public IMinigame Focused => _games[FocusIndex];
        public IReadOnlyList<IMinigame> Games => _games;

        public SwitchEngine(IReadOnlyList<IMinigame> games, SwitchTimer timer, MatchState state)
        {
            if (games == null || games.Count == 0)
                throw new ArgumentException("至少要有一個小遊戲", nameof(games));
            _games = games;
            Timer = timer ?? throw new ArgumentNullException(nameof(timer));
            State = state ?? throw new ArgumentNullException(nameof(state));

            Timer.OnSwitch += SwitchFocus;
            foreach (var g in _games) g.OnFail += HandleFail;
            FocusIndex = 0;
        }

        public void Start()
        {
            foreach (var g in _games) { g.Init(); g.Reset(); }
            for (int i = 0; i < _games.Count; i++)
            {
                if (i == FocusIndex) _games[i].OnFocus();
                else _games[i].OnBlur();
            }
        }

        public void Tick(float dt)
        {
            if (State.IsGameOver) return;
            State.TickTime(dt);
            Timer.Tick(dt); // 可能透過事件呼叫 SwitchFocus
            for (int i = 0; i < _games.Count; i++)
                _games[i].Tick(dt, i == FocusIndex);
        }

        private void SwitchFocus()
        {
            _games[FocusIndex].OnBlur();
            FocusIndex = (FocusIndex + 1) % _games.Count;
            _games[FocusIndex].OnFocus();
        }

        private void HandleFail(IMinigame game)
        {
            State.LoseHp(1);
            game.Reset();
        }
    }
}
```

- [ ] **Step 4：跑測試確認通過**
- [ ] **Step 5：Commit**
```bash
git add Assets/Scripts/Core/SwitchEngine.cs Assets/Tests/EditMode/SwitchEngineTests.cs
git commit -m "feat(core): SwitchEngine orchestrates timer, focus, fail"
```

---

## Task 5：DinoSim（反射小遊戲，TDD）

**Files:**
- Create: `Assets/Scripts/Minigames/DinoSim.cs`
- Test: `Assets/Tests/EditMode/DinoSimTests.cs`

- [ ] **Step 1：寫失敗測試**
```csharp
using NUnit.Framework;
using PanicConsole.Core;
using PanicConsole.Minigames;

public class DinoSimTests
{
    [Test] public void ResetPlacesObstacleAtSpawn()
    {
        var d = new DinoSim();
        d.Reset();
        Assert.AreEqual(d.SpawnDistance, d.ObstacleX, 0.001f);
    }

    [Test] public void FocusedTickMovesObstacleLeft()
    {
        var d = new DinoSim(); d.Reset(); d.OnFocus();
        d.Tick(1f, true);
        Assert.Less(d.ObstacleX, d.SpawnDistance);
    }

    [Test] public void BackgroundTickFreezesObstacle()
    {
        var d = new DinoSim(); d.Reset(); d.OnBlur();
        d.Tick(1f, false);
        Assert.AreEqual(d.SpawnDistance, d.ObstacleX, 0.001f);
    }

    [Test] public void ObstacleReachingPlayerWhileGroundedFails()
    {
        var d = new DinoSim(); d.Reset(); d.OnFocus();
        bool failed = false; d.OnFail += _ => failed = true;
        d.Tick(100f, true); // obstacle definitely reaches 0, not jumping
        Assert.IsTrue(failed);
    }

    [Test] public void JumpingClearsObstacleWithoutFail()
    {
        var d = new DinoSim { SpawnDistance = 6f, ScrollSpeed = 6f };
        d.Reset(); d.OnFocus();
        bool failed = false; d.OnFail += _ => failed = true;
        d.Jump();               // airborne for JumpDuration
        d.Tick(1f, true);       // obstacle travels 6 units to 0 while airborne
        Assert.IsFalse(failed);
        Assert.AreEqual(d.SpawnDistance, d.ObstacleX, 0.001f); // respawned
    }
}
```

- [ ] **Step 2：跑測試確認失敗** — 預期 `DinoSim` 未定義。

- [ ] **Step 3：實作**
```csharp
using PanicConsole.Core;

namespace PanicConsole.Minigames
{
    /// <summary>反射神經：地面捲動、按跳躲避障礙。OnBlur 完全凍結。</summary>
    public class DinoSim : MinigameBase
    {
        public float SpawnDistance = 20f;
        public float ScrollSpeed = 8f;
        public float JumpDuration = 0.5f;

        public float ObstacleX { get; private set; }
        public bool IsAirborne => _airborne > 0f;
        private float _airborne;

        public DinoSim() { GameId = "dino"; }

        public override void Reset()
        {
            ObstacleX = SpawnDistance;
            _airborne = 0f;
        }

        public void Jump()
        {
            if (_airborne <= 0f) _airborne = JumpDuration;
        }

        protected override void Simulate(float dt, bool isFocused)
        {
            if (!isFocused) return; // 背景凍結

            bool airborneThisFrame = _airborne > 0f; // 以「進入此幀時是否在空中」判定通過
            ObstacleX -= ScrollSpeed * dt;
            if (ObstacleX <= 0f)
            {
                if (airborneThisFrame) ObstacleX = SpawnDistance; // 安全通過，重生
                else TriggerFail();
            }
            if (_airborne > 0f) _airborne -= dt;
        }
    }
}
```

- [ ] **Step 4：跑測試確認通過**
- [ ] **Step 5：Commit**
```bash
git add Assets/Scripts/Minigames/DinoSim.cs Assets/Tests/EditMode/DinoSimTests.cs
git commit -m "feat(minigame): DinoSim reflex game"
```

---

## Task 6：SnakeSim（空間小遊戲，TDD）

**Files:**
- Create: `Assets/Scripts/Minigames/SnakeSim.cs`
- Test: `Assets/Tests/EditMode/SnakeSimTests.cs`

- [ ] **Step 1：寫失敗測試**
```csharp
using NUnit.Framework;
using PanicConsole.Core;
using PanicConsole.Minigames;

public class SnakeSimTests
{
    [Test] public void ResetCentersSnakeMovingRight()
    {
        var s = new SnakeSim(); s.Reset();
        Assert.AreEqual(3, s.Body.Count);
        Assert.AreEqual(1, s.DirX); Assert.AreEqual(0, s.DirY);
    }

    [Test] public void FocusedStepMovesHeadRight()
    {
        var s = new SnakeSim(); s.Reset(); s.OnFocus();
        var head = s.Body[0];
        s.Tick(s.StepInterval, true);
        Assert.AreEqual(head.X + 1, s.Body[0].X);
        Assert.AreEqual(head.Y, s.Body[0].Y);
    }

    [Test] public void BackgroundMovesAtQuarterSpeed()
    {
        var s = new SnakeSim(); s.Reset(); s.OnBlur();
        var head = s.Body[0];
        s.Tick(s.StepInterval, false);        // 0.25 step worth -> no move yet
        Assert.AreEqual(head.X, s.Body[0].X);
        s.Tick(s.StepInterval * 3f, false);   // total 1 step worth -> one move
        Assert.AreEqual(head.X + 1, s.Body[0].X);
    }

    [Test] public void HittingWallFails()
    {
        var s = new SnakeSim { Width = 10, Height = 10 }; s.Reset(); s.OnFocus();
        bool failed = false; s.OnFail += _ => failed = true;
        for (int i = 0; i < 20 && !failed; i++) s.Tick(s.StepInterval, true);
        Assert.IsTrue(failed);
    }

    [Test] public void CannotReverseDirection()
    {
        var s = new SnakeSim(); s.Reset(); s.OnFocus(); // moving right
        s.SetDirection(-1, 0); // try reverse -> ignored
        Assert.AreEqual(1, s.DirX);
    }
}
```

- [ ] **Step 2：跑測試確認失敗** — 預期 `SnakeSim` 未定義。

- [ ] **Step 3：實作**
```csharp
using System.Collections.Generic;
using PanicConsole.Core;

namespace PanicConsole.Minigames
{
    /// <summary>空間規劃：方向鍵移動的貪食蛇。OnBlur 以 25% 速度沿當前方向直行。</summary>
    public class SnakeSim : MinigameBase
    {
        public int Width = 15;
        public int Height = 15;
        public float StepInterval = 0.15f;

        public struct Cell { public int X, Y; public Cell(int x, int y){X=x;Y=y;} }

        private readonly List<Cell> _body = new List<Cell>();
        public IReadOnlyList<Cell> Body => _body;
        public int DirX { get; private set; }
        public int DirY { get; private set; }
        public Cell Food { get; private set; }

        private float _acc;
        private int _foodSeq;

        public SnakeSim() { GameId = "snake"; }

        public override void Reset()
        {
            _body.Clear();
            int cx = Width / 2, cy = Height / 2;
            _body.Add(new Cell(cx, cy));
            _body.Add(new Cell(cx - 1, cy));
            _body.Add(new Cell(cx - 2, cy));
            DirX = 1; DirY = 0;
            _acc = 0f; _foodSeq = 0;
            RespawnFood();
        }

        public void SetDirection(int dx, int dy)
        {
            if (dx == -DirX && dy == -DirY) return; // 不可反向
            if (dx != 0 && dy != 0) return;         // 只允許四方向
            DirX = dx; DirY = dy;
        }

        protected override void Simulate(float dt, bool isFocused)
        {
            float factor = isFocused ? 1f : 0.25f;
            _acc += dt * factor;
            while (_acc >= StepInterval)
            {
                _acc -= StepInterval;
                if (!Step()) { _acc = 0f; break; }
            }
        }

        private bool Step()
        {
            var head = _body[0];
            int nx = head.X + DirX, ny = head.Y + DirY;
            if (nx < 0 || nx >= Width || ny < 0 || ny >= Height) { TriggerFail(); return false; }
            for (int i = 0; i < _body.Count - 1; i++) // 撞自己（尾巴會讓位故排除）
                if (_body[i].X == nx && _body[i].Y == ny) { TriggerFail(); return false; }

            _body.Insert(0, new Cell(nx, ny));
            if (nx == Food.X && ny == Food.Y) RespawnFood();
            else _body.RemoveAt(_body.Count - 1);
            return true;
        }

        private void RespawnFood()
        {
            // 決定性擺放（便於測試）；遊戲中視覺仍會四處出現
            _foodSeq++;
            int fx = (Food.X + 3 + _foodSeq) % Width;
            int fy = (Food.Y + 2 + _foodSeq) % Height;
            if (fx == 0 && fy == 0) fx = Width / 2;
            Food = new Cell(fx, fy);
        }
    }
}
```

- [ ] **Step 4：跑測試確認通過**
- [ ] **Step 5：Commit**
```bash
git add Assets/Scripts/Minigames/SnakeSim.cs Assets/Tests/EditMode/SnakeSimTests.cs
git commit -m "feat(minigame): SnakeSim spatial game"
```

---

## Task 7：PianoSim（節奏小遊戲，TDD）

**Files:**
- Create: `Assets/Scripts/Minigames/PianoSim.cs`
- Test: `Assets/Tests/EditMode/PianoSimTests.cs`

- [ ] **Step 1：寫失敗測試**
```csharp
using NUnit.Framework;
using PanicConsole.Core;
using PanicConsole.Minigames;

public class PianoSimTests
{
    [Test] public void ResetSpawnsOneTileAtTop()
    {
        var p = new PianoSim(); p.Reset();
        Assert.AreEqual(1, p.Tiles.Count);
        Assert.AreEqual(p.Height, p.Tiles[0].Y, 0.001f);
    }

    [Test] public void BackgroundTickFreezesTiles()
    {
        var p = new PianoSim(); p.Reset(); p.OnBlur();
        float y = p.Tiles[0].Y;
        p.Tick(1f, false);
        Assert.AreEqual(y, p.Tiles[0].Y, 0.001f);
        Assert.AreEqual(1, p.Tiles.Count); // 不生成
    }

    [Test] public void FocusedTickMovesTilesDown()
    {
        var p = new PianoSim(); p.Reset(); p.OnFocus();
        float y = p.Tiles[0].Y;
        p.Tick(0.05f, true);
        Assert.Less(p.Tiles[0].Y, y);
    }

    [Test] public void TileReachingBottomFails()
    {
        var p = new PianoSim(); p.Reset(); p.OnFocus();
        bool failed = false; p.OnFail += _ => failed = true;
        p.Tick(100f, true);
        Assert.IsTrue(failed);
    }

    [Test] public void HitRemovesTileInColumn()
    {
        var p = new PianoSim(); p.Reset(); p.OnFocus();
        int col = p.Tiles[0].Column;
        p.Hit(col);
        Assert.AreEqual(0, p.Tiles.Count);
    }

    [Test] public void HitEmptyColumnFails()
    {
        var p = new PianoSim(); p.Reset(); p.OnFocus();
        int occupied = p.Tiles[0].Column;
        int empty = (occupied + 1) % p.Columns;
        bool failed = false; p.OnFail += _ => failed = true;
        p.Hit(empty);
        Assert.IsTrue(failed);
    }
}
```

- [ ] **Step 2：跑測試確認失敗** — 預期 `PianoSim` 未定義。

- [ ] **Step 3：實作**
```csharp
using System.Collections.Generic;
using PanicConsole.Core;

namespace PanicConsole.Minigames
{
    /// <summary>節奏精準：黑塊由上落下，需在落地前點掉正確直行。OnBlur 凍結。</summary>
    public class PianoSim : MinigameBase
    {
        public int Columns = 4;
        public float Height = 10f;
        public float FallSpeed = 4f;
        public float SpawnInterval = 0.8f;

        public class Tile { public int Column; public float Y; }

        private readonly List<Tile> _tiles = new List<Tile>();
        public IReadOnlyList<Tile> Tiles => _tiles;

        private float _spawnAcc;
        private int _spawnSeq;

        public PianoSim() { GameId = "piano"; }

        public override void Reset()
        {
            _tiles.Clear();
            _spawnAcc = 0f; _spawnSeq = 0;
            Spawn(); // 起始給一塊在頂端
        }

        public void Hit(int column)
        {
            // 找該行最接近底部（Y 最小）的方塊
            int idx = -1; float min = float.MaxValue;
            for (int i = 0; i < _tiles.Count; i++)
                if (_tiles[i].Column == column && _tiles[i].Y < min) { min = _tiles[i].Y; idx = i; }

            if (idx < 0) { TriggerFail(); return; } // 點了空行
            _tiles.RemoveAt(idx);
        }

        protected override void Simulate(float dt, bool isFocused)
        {
            if (!isFocused) return; // 背景凍結

            _spawnAcc += dt;
            while (_spawnAcc >= SpawnInterval) { _spawnAcc -= SpawnInterval; Spawn(); }

            for (int i = 0; i < _tiles.Count; i++) _tiles[i].Y -= FallSpeed * dt;

            for (int i = 0; i < _tiles.Count; i++)
                if (_tiles[i].Y <= 0f) { TriggerFail(); return; }
        }

        private void Spawn()
        {
            _tiles.Add(new Tile { Column = _spawnSeq % Columns, Y = Height });
            _spawnSeq++;
        }
    }
}
```

- [ ] **Step 4：跑測試確認通過**
- [ ] **Step 5：Commit**
```bash
git add Assets/Scripts/Minigames/PianoSim.cs Assets/Tests/EditMode/PianoSimTests.cs
git commit -m "feat(minigame): PianoSim rhythm game"
```

---

## Task 8：MonoBehaviour 編排殼（SwitchController / GameSession / InputRouter）

> 此後為 Unity 執行期程式，無法純單元測試；改以 **Play 模式手動驗證清單**。

**Files:**
- Create: `Assets/Scripts/App/SwitchController.cs`
- Create: `Assets/Scripts/App/InputRouter.cs`

- [ ] **Step 1：實作 SwitchController**
```csharp
using System.Collections.Generic;
using PanicConsole.Core;
using PanicConsole.Minigames;
using UnityEngine;

namespace PanicConsole.App
{
    /// <summary>建立純邏輯 SwitchEngine 並每幀驅動；持有可調切換秒數。</summary>
    public class SwitchController : MonoBehaviour
    {
        public SwitchEngine Engine { get; private set; }
        public System.Action<int> OnFocusChanged; // 給 view 訂閱

        private InputRouter _input;
        private int _lastFocus = -1;

        public IReadOnlyList<IMinigame> Games => Engine.Games;

        void Awake()
        {
            var games = new List<IMinigame> { new DinoSim(), new SnakeSim(), new PianoSim() };
            Engine = new SwitchEngine(games, new SwitchTimer(), new MatchState());
            _input = GetComponent<InputRouter>();
            Engine.Start();
        }

        void Update()
        {
            float dt = Time.deltaTime;
            if (Engine.FocusIndex != _lastFocus)
            {
                _lastFocus = Engine.FocusIndex;
                OnFocusChanged?.Invoke(_lastFocus);
            }
            _input?.RouteTo(Engine.Focused); // 只把輸入給前台
            Engine.Tick(dt);
        }
    }
}
```

- [ ] **Step 2：實作 InputRouter**
```csharp
using PanicConsole.Core;
using PanicConsole.Minigames;
using UnityEngine;

namespace PanicConsole.App
{
    /// <summary>只把玩家輸入轉給前台小遊戲。未來「反轉/禁用輸入」卡牌的插入點。</summary>
    public class InputRouter : MonoBehaviour
    {
        public void RouteTo(IMinigame focused)
        {
            switch (focused)
            {
                case DinoSim dino:
                    if (Input.GetKeyDown(KeyCode.Space) || Input.GetKeyDown(KeyCode.UpArrow)) dino.Jump();
                    break;
                case SnakeSim snake:
                    if (Input.GetKeyDown(KeyCode.UpArrow)) snake.SetDirection(0, 1);
                    else if (Input.GetKeyDown(KeyCode.DownArrow)) snake.SetDirection(0, -1);
                    else if (Input.GetKeyDown(KeyCode.LeftArrow)) snake.SetDirection(-1, 0);
                    else if (Input.GetKeyDown(KeyCode.RightArrow)) snake.SetDirection(1, 0);
                    break;
                case PianoSim piano:
                    if (Input.GetKeyDown(KeyCode.A)) piano.Hit(0);
                    else if (Input.GetKeyDown(KeyCode.S)) piano.Hit(1);
                    else if (Input.GetKeyDown(KeyCode.D)) piano.Hit(2);
                    else if (Input.GetKeyDown(KeyCode.F)) piano.Hit(3);
                    break;
            }
        }
    }
}
```
> 注意：Snake 在背景不該轉向——因為 `RouteTo` 只對 `Engine.Focused` 呼叫，背景遊戲天然收不到輸入。

- [ ] **Step 3：手動驗證（暫以 log）**
在 `SwitchController.Update` 末尾暫加 `if (Engine.State.IsGameOver) Debug.Log("GAME OVER");`。建一個空場景掛上 `SwitchController` + `InputRouter`，按 Play，觀察 Console 約 20 秒後不報錯（焦點切換的視覺在 Task 9 才有）。確認後移除暫加的 log。

- [ ] **Step 4：Commit**
```bash
git add Assets/Scripts/App/SwitchController.cs Assets/Scripts/App/InputRouter.cs
git commit -m "feat(app): SwitchController and InputRouter MonoBehaviours"
```

---

## Task 9：小遊戲 View（渲染薄殼 + RenderTexture）

**Files:**
- Create: `Assets/Scripts/Views/MinigameView.cs`
- Create: `Assets/Scripts/Views/DinoView.cs`
- Create: `Assets/Scripts/Views/SnakeView.cs`
- Create: `Assets/Scripts/Views/PianoView.cs`

- [ ] **Step 1：實作 MinigameView 抽象基底**
```csharp
using PanicConsole.Core;
using UnityEngine;

namespace PanicConsole.Views
{
    /// <summary>持有一個 IMinigame，每幀依其狀態繪製。實作以簡單圖元為主（切片不求美術）。</summary>
    public abstract class MinigameView : MonoBehaviour
    {
        protected IMinigame Game;
        public void Bind(IMinigame game) => Game = game;
        protected virtual void LateUpdate() { if (Game != null) Draw(); }
        protected abstract void Draw();
    }
}
```

- [ ] **Step 2：實作三個 View（以基本圖元呈現狀態）**
`DinoView.cs`（用兩個 `Transform` 代表恐龍與障礙；依 `ObstacleX` 與 `IsAirborne` 擺位）
```csharp
using PanicConsole.Minigames;
using UnityEngine;

namespace PanicConsole.Views
{
    public class DinoView : MinigameView
    {
        public Transform Player;   // 在 Inspector 指定
        public Transform Obstacle;
        public float Lane = 0f, GroundY = 0f, JumpY = 1.5f, WorldScale = 0.4f;

        protected override void Draw()
        {
            var d = (DinoSim)Game;
            if (Obstacle) Obstacle.localPosition = new Vector3(d.ObstacleX * WorldScale, GroundY, 0);
            if (Player) Player.localPosition = new Vector3(Lane, d.IsAirborne ? JumpY : GroundY, 0);
        }
    }
}
```
`SnakeView.cs`（用物件池畫格子；簡化為依 `Body` 重畫方塊）
```csharp
using System.Collections.Generic;
using PanicConsole.Minigames;
using UnityEngine;

namespace PanicConsole.Views
{
    public class SnakeView : MinigameView
    {
        public GameObject CellPrefab;     // 小方塊 prefab
        public GameObject FoodPrefab;
        public float Cell = 0.3f;
        private readonly List<GameObject> _pool = new List<GameObject>();
        private GameObject _food;

        protected override void Draw()
        {
            var s = (SnakeSim)Game;
            while (_pool.Count < s.Body.Count) _pool.Add(Instantiate(CellPrefab, transform));
            for (int i = 0; i < _pool.Count; i++)
            {
                bool on = i < s.Body.Count;
                _pool[i].SetActive(on);
                if (on) _pool[i].transform.localPosition = new Vector3(s.Body[i].X * Cell, s.Body[i].Y * Cell, 0);
            }
            if (_food == null) _food = Instantiate(FoodPrefab, transform);
            _food.transform.localPosition = new Vector3(s.Food.X * Cell, s.Food.Y * Cell, 0);
        }
    }
}
```
`PianoView.cs`（依 `Tiles` 物件池繪製落下方塊）
```csharp
using System.Collections.Generic;
using PanicConsole.Minigames;
using UnityEngine;

namespace PanicConsole.Views
{
    public class PianoView : MinigameView
    {
        public GameObject TilePrefab;
        public float ColWidth = 0.5f, YScale = 0.4f;
        private readonly List<GameObject> _pool = new List<GameObject>();

        protected override void Draw()
        {
            var p = (PianoSim)Game;
            while (_pool.Count < p.Tiles.Count) _pool.Add(Instantiate(TilePrefab, transform));
            for (int i = 0; i < _pool.Count; i++)
            {
                bool on = i < p.Tiles.Count;
                _pool[i].SetActive(on);
                if (on) _pool[i].transform.localPosition =
                    new Vector3(p.Tiles[i].Column * ColWidth, p.Tiles[i].Y * YScale, 0);
            }
        }
    }
}
```

- [ ] **Step 3：RenderTexture 與相機（場景設定，手動）**
每個 view 放在各自圖層、各自 `Camera`，相機 `Target Texture` 指向一張 `RenderTexture`，再以 `RawImage` 顯示在控制台 UI（前台大、其餘小）。這部分在 Task 10 的場景組裝一起完成。

- [ ] **Step 4：Commit**
```bash
git add Assets/Scripts/Views
git commit -m "feat(views): minigame views with primitive rendering"
```

---

## Task 10：UI、Bootstrap 與場景組裝

**Files:**
- Create: `Assets/Scripts/App/DashboardUI.cs`
- Create: `Assets/Scripts/App/DebugHud.cs`
- Create: `Assets/Scripts/App/GameBootstrap.cs`
- Create: `Assets/Scenes/Slice.unity`

- [ ] **Step 1：DashboardUI（顯示 HP / 計時 / 分數 / 警報）**
```csharp
using PanicConsole.App;
using UnityEngine;
using UnityEngine.UI;

namespace PanicConsole.App
{
    public class DashboardUI : MonoBehaviour
    {
        public SwitchController Controller;
        public Text HpText, TimeText, RoundText, WarnText;

        void Update()
        {
            if (Controller?.Engine == null) return;
            var e = Controller.Engine;
            if (HpText)   HpText.text   = $"HP {e.State.Hp}/{e.State.MaxHp}";
            if (TimeText) TimeText.text = $"{e.State.SurvivalTime:0.0}s";
            if (RoundText)RoundText.text= $"Round {e.Timer.Round}  ({e.Timer.Remaining:0.0}s)";
            if (WarnText) WarnText.enabled = e.Timer.WarningActive && !e.State.IsGameOver;
        }
    }
}
```

- [ ] **Step 2：DebugHud（即時調整切換秒數，找好玩的數字）**
```csharp
using UnityEngine;

namespace PanicConsole.App
{
    /// <summary>切片專用：用 OnGUI 滑桿調整警報秒數與重開，方便試玩調校。</summary>
    public class DebugHud : MonoBehaviour
    {
        public SwitchController Controller;
        private float _warn = 3f;

        void OnGUI()
        {
            GUILayout.BeginArea(new Rect(10, 10, 260, 90), GUI.skin.box);
            GUILayout.Label($"Warning seconds: {_warn:0.0}");
            _warn = GUILayout.HorizontalSlider(_warn, 1f, 6f);
            if (GUILayout.Button("Restart")) UnityEngine.SceneManagement.SceneManager.LoadScene(0);
            GUILayout.EndArea();
        }
    }
}
```
> 切換間隔曲線（20/15/10）直接在 `SwitchTimer.IntervalForRound` 改即可；如需 Play 中即時調，於 `SwitchController` 暴露倍率欄位（YAGNI：先用滑桿調警報＋重開驗證節奏即可）。

- [ ] **Step 3：GameBootstrap（把 SwitchEngine 的小遊戲綁到對應 view）**
```csharp
using PanicConsole.Minigames;
using PanicConsole.Views;
using UnityEngine;

namespace PanicConsole.App
{
    public class GameBootstrap : MonoBehaviour
    {
        public SwitchController Controller;
        public DinoView DinoView;
        public SnakeView SnakeView;
        public PianoView PianoView;

        void Start()
        {
            foreach (var g in Controller.Games)
            {
                switch (g)
                {
                    case DinoSim d:  DinoView.Bind(d); break;
                    case SnakeSim s: SnakeView.Bind(s); break;
                    case PianoSim p: PianoView.Bind(p); break;
                }
            }
        }
    }
}
```
> `GameBootstrap.Start()` 在 `SwitchController.Awake()` 之後執行，故 `Controller.Games` 已建立。

- [ ] **Step 4：場景組裝（手動）**
1. 新場景 `Slice.unity`。
2. 空物件 `GameRoot`，掛 `SwitchController` + `InputRouter` + `GameBootstrap` + `DashboardUI` + `DebugHud`。
3. 三個小遊戲各一個父物件 + 一台 `Camera`（Target Texture 指各自 RenderTexture）+ 對應 View 與其 prefab/Transform。
4. Canvas 上放三個 `RawImage`（前台大、兩個小縮圖）顯示三張 RenderTexture，以及 HP/Time/Round/Warn 的 `Text`。
5. `GameBootstrap` 與 `DashboardUI` 的欄位在 Inspector 指好。
6. `OnFocusChanged` 由一個小腳本或 DashboardUI 訂閱，切換哪張 RawImage 放大（可後續微調）。

- [ ] **Step 5：Commit**
```bash
git add Assets/Scripts/App Assets/Scenes
git commit -m "feat(app): dashboard, debug HUD, bootstrap, slice scene"
```

---

## Task 11：手動試玩驗證與調校（Phase 0 的驗收關卡）

**Files:** 無（驗證與調參）

- [ ] **Step 1：跑全部 EditMode 測試** — `Test Runner > Run All`，全綠。
- [ ] **Step 2：Play 冒煙測試清單**
  - [ ] 三個小遊戲同時在動（含背景：蛇慢速前進、恐龍/鋼琴凍結）。
  - [ ] 每 20 秒焦點切換，倒數 3 秒有警報提示。
  - [ ] 任一小遊戲失誤 → HP 減 1 且該遊戲重置。
  - [ ] HP 歸零 → 顯示 Game Over / 可重開。
  - [ ] 前台才吃輸入，背景不吃。
- [ ] **Step 3：好玩度評估（成功標準，見 spec 第 3 節）**
  - [ ] 試玩者會緊張、想「再來一局」。
  - [ ] 旁觀者光看畫面就能感受到混亂感。
  - [ ] 用 DebugHud 調整節奏，記下「最好玩」的設定。
- [ ] **Step 4：把調校後的數字回填 `定案規格.md` 第 3 區。**
- [ ] **Step 5：Commit**
```bash
git add 定案規格.md
git commit -m "docs(slice): record playtest-tuned timing values"
```

---

## Self-Review（對照 spec）

- **Spec 第 1/3 節（目標/成功標準）** → Task 11 驗收關卡涵蓋。
- **第 4.1 MinigameBase / 純邏輯** → Task 3（精修為純 C#，見開頭說明）。
- **第 4.2 SwitchController/切換/警報** → Task 1（Timer）+ Task 4（Engine）+ Task 8（MonoBehaviour）。
- **第 4.3 GameSession/HP/Reset** → Task 2（MatchState）+ Task 4（HandleFail）。
- **第 4.4 InputRouter（只送前台、卡牌插入點）** → Task 8。
- **第 4.5 渲染/UI（RenderTexture、前台大縮圖小、Debug HUD）** → Task 9 + Task 10。
- **第 5 資料流** → Task 4（Engine.Tick）+ Task 8（Update 順序）。
- **第 6 數值預設（HP5、20/15/10、3秒警報、0.5秒預警、3並行）** → HP5/曲線/警報已於 Task 1/2 落實；3 並行於 Task 8。**0.5 秒重新聚焦預警**：本切片各 sim 的 `OnFocus` 暫未實作預警緩衝（DinoSim/PianoSim 背景為凍結、Snake 為慢速，切回不致瞬死），列為 Task 11 試玩後若有「切回瞬死」感才補的微調項，不阻擋驗證。
- **第 7 三個小遊戲（含 OnFocus/OnBlur 行為、失敗條件）** → Task 5/6/7。
- **第 9 測試策略** → 各 TDD task + Task 11 Step 1。
- **第 10 專案結構** → File Structure（asmdef 化，較 spec 更細）。

placeholder 掃描：無 TBD/TODO；型別與方法名跨 task 一致（`Tick(dt,isFocused)`、`TriggerFail`、`SetDirection`、`Hit`、`Jump`、`Engine.State`、`Timer.Round` 等）。
